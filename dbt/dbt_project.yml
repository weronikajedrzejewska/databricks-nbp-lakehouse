name: nbp_dbt
version: "1.0"
profile: nbp_dbt

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  nbp_dbt:
    +materialized: view